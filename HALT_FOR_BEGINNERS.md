# HALTを調べてみよう

このメモは、`HALT` というCPU命令を、中学生でも分かるくらい小さく分解して理解するための説明です。

## HALTとは

`HALT` は、CPUに対して、

```text
もう動かなくていいよ
```

と伝える命令です。

ゲームでいうと「終了ボタン」、動画でいうと「停止ボタン」に近いです。

CPUはふつう、メモリから命令を1つずつ読んで、ずっと動き続けます。

```text
命令を読む
命令を実行する
次の命令を読む
次の命令を実行する
...
```

でも、どこかで止まらないとプログラムが終わりません。

そこで `HALT` を使います。

## CPUは数字しか読めない

人間はこう書きます。

```asm
HALT
```

でもCPUは `HALT` という文字を読めません。

CPUが読めるのは数字だけです。

この自作CPUでは、`HALT` を次の数字で表すことにします。

```text
0x01000000
```

`0x` は「16進数ですよ」という意味です。

つまり、

```text
HALT = 0x01000000
```

と決めます。

## ファイルの中では4つのバイトになる

このCPUの命令は、1つにつき4バイトです。

`HALT` は、

```text
0x01000000
```

なので、ファイルの中では次の4つの数字になります。

```text
01 00 00 00
```

これは、

```text
1個目のバイト: 01
2個目のバイト: 00
3個目のバイト: 00
4個目のバイト: 00
```

という意味です。

## PCとは

CPUには `PC` という大事なレジスタがあります。

`PC` は `Program Counter` の略です。

簡単にいうと、

```text
次に読む命令が、メモリのどこにあるか
```

を覚えている数字です。

たとえば、

```text
PC = 0x00000000
```

なら、CPUはメモリの `0x00000000` 番地から命令を読みます。

## HALTを読む流れ

メモリにこう入っているとします。

```text
番地       中身
0x00000000   01
0x00000001   00
0x00000002   00
0x00000003   00
```

CPUは `PC = 0x00000000` から4バイト読みます。

すると、

```text
01 00 00 00
```

を読みます。

これは1つの命令として見ると、

```text
0x01000000
```

です。

つまり `HALT` です。

## 命令を2つに分けて見る

`0x01000000` の一番最初のバイトは、

```text
0x01
```

です。

この `0x01` を、さらに2つに分けます。

```text
0x01
```

は、次のように見ます。

```text
上半分: 0
下半分: 1
```

このCPUでは、

```text
上半分 = 命令の種類
下半分 = その種類の中の番号
```

として使います。

つまり `HALT` は、

```text
命令の種類: 0
命令番号: 1
```

です。

もう少し専門的に書くと、

```text
type = 0
op   = 1
```

です。

## PCは4進む

このCPUの命令は1つ4バイトです。

だから、命令を1つ読んだら、`PC` は4進みます。

```text
読む前: PC = 0x00000000
読む後: PC = 0x00000004
```

ただし、`HALT` はCPUを止める命令です。

なので、次の命令を読む前にCPUは止まります。

## HALTの動き

`HALT` を実行したら、やることはとても少ないです。

```text
CPUを止める
```

これだけです。

レジスタの値を変えたり、メモリに何かを書いたりはしません。

## 最小テスト

最初のテストは、`HALT` だけで十分です。

やりたいこと:

```text
メモリにHALTを置く
CPUに読ませる
CPUが止まるか確認する
```

メモリ:

```text
0x00000000: 01
0x00000001: 00
0x00000002: 00
0x00000003: 00
```

期待する結果:

```text
CPU halted.
```

と表示されて、プログラムが終わる。

## 調べるときの順番

難しく感じたら、次の順番で見ます。

```text
1. HALTは何をする命令か
2. HALTはどんな数字で表すか
3. ファイルの中では何バイトになるか
4. PCはどこを指しているか
5. CPUは何を読んだか
6. 読んだ命令をtypeとopに分けられるか
7. type=0, op=1なら止まるか
```

## まとめ

`HALT` は、CPUを止める命令です。

この自作CPUでは、`HALT` を次の数字で表します。

```text
0x01000000
```

ファイルの中では、次の4バイトです。

```text
01 00 00 00
```

CPUは `PC` が指す場所からこの4バイトを読みます。

そして、

```text
type = 0
op = 1
```

だと分かったら、

```text
CPU halted.
```

と表示して止まります。

まずはこれだけ理解できれば十分です。
