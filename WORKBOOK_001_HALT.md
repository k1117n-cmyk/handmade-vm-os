# Workbook 001: HALTを手書きで実装する

このファイルは、`HALT` 命令を実際に手書きで作るための作業メモです。

完成コードを読む前に、この順番で進める。

## 今日のゴール

```text
HALT命令だけをメモリに置き、CPUがそれを読んで停止する。
```

期待する最後の表示:

```text
CPU halted.
```

## 作るもの

最小のVMを1つ作る。

最初はファイル分割しなくてよい。

```text
handmade-vm-practice/
  vm.c
```

または、既存の練習場所を使うなら:

```text
handmade-vm/
  src/
    vm.c
```

## まだ作らないもの

今回は以下は作らない。

```text
アセンブラ
MOVI
SYSCALL
文字列表示
ファイル読み込み
シェル
エディター
```

まず `HALT` だけに集中する。

## Step 1: CPUの状態を決める

VMに必要な状態は、最初は2つだけ。

```text
memory
PC
```

今回の仕様:

```text
memory size: 1MB
memory type: 1バイトずつ入る配列
PC initial value: 0x00000000
```

メモ:

```text
memoryは、CPUが読む命令やデータを置く場所。
PCは、次に読む命令の場所を覚える数字。
```

確認したいこと:

```text
PCの初期値を表示できるか。
```

期待:

```text
PC=00000000
```

## Step 2: メモリにHALTを置く

`HALT` の機械語は:

```text
0x01000000
```

ファイルやメモリ上では4バイト:

```text
01 00 00 00
```

手でメモリに置く。

```text
memory[0x00000000] = 0x01
memory[0x00000001] = 0x00
memory[0x00000002] = 0x00
memory[0x00000003] = 0x00
```

確認:

```text
メモリの4バイトを表示する。
```

期待:

```text
01 00 00 00
```

## Step 3: 4バイトを1つの命令として読む

CPUは4バイトをまとめて1命令として読む。

今回読みたい値:

```text
01 00 00 00
```

これを1つの32bitの数字にすると:

```text
0x01000000
```

この読み方を big-endian と呼ぶ。

考え方:

```text
1個目のバイトを一番左に置く
2個目のバイトをその次に置く
3個目のバイトをその次に置く
4個目のバイトを一番右に置く
```

確認:

```text
fetchした命令を表示する。
```

期待:

```text
INST=01000000
```

## Step 4: PCを4進める

このCPUの命令は1つ4バイト。

だから、命令を読んだら `PC` を4進める。

```text
読む前: PC = 0x00000000
読む後: PC = 0x00000004
```

確認:

```text
fetch前とfetch後のPCを表示する。
```

期待:

```text
PC before=00000000
PC after =00000004
```

## Step 5: 命令をtypeとopに分ける

命令:

```text
0x01000000
```

最初の1バイト:

```text
0x01
```

これを2つに分ける。

```text
type = 上半分 = 0
op   = 下半分 = 1
```

確認:

```text
typeとopを表示する。
```

期待:

```text
TYPE=0
OP=1
```

## Step 6: HALTとして実行する

今回のルール:

```text
type=0 かつ op=1 なら HALT
```

HALTだったら、実行ループを止める。

表示:

```text
CPU halted.
```

## 最小実行ログの例

自分のVMがこのようなログを出せれば、かなり理解できている。

```text
PC before=00000000
INST=01000000
PC after =00000004
TYPE=0
OP=1
CPU halted.
```

## チェックリスト

作業後に確認する。

```text
[ ] memoryを1MB用意した
[ ] PCを0x00000000にした
[ ] memory[0x00000000]から01 00 00 00を置いた
[ ] 4バイトを0x01000000として読めた
[ ] fetch後にPCが0x00000004になった
[ ] type=0を取り出せた
[ ] op=1を取り出せた
[ ] type=0, op=1でCPUを止めた
[ ] CPU halted. と表示された
```

## よくあるミス

### 1. PCを1だけ進めてしまう

今回の命令は4バイトなので、PCは4進める。

```text
正しい: PC += 4
間違い: PC += 1
```

### 2. バイトの順番を逆に読む

メモリ:

```text
01 00 00 00
```

正しい命令:

```text
0x01000000
```

間違った読み方:

```text
0x00000001
```

### 3. typeとopを逆にする

`0x01` の上半分が `type`、下半分が `op`。

```text
type = 0
op   = 1
```

### 4. HALT後もループを続けてしまう

HALTを見つけたら、実行ループを止める。

## 自分で説明できるか

最後に、何も見ずに次の文を言えるか確認する。

```text
HALTは0x01000000という4バイト命令で、
CPUはPCが指す場所からそれを読み、
type=0, op=1だと分かったら実行ループを止める。
```

これが言えれば、次の `MOVI` に進んでよい。
